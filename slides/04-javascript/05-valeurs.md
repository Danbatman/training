# Valeurs

* number
* string
* boolean (`true`, `false`)
* null
* undefined
* object (`Object(x) === x`)
    * function (`typeof f === 'function'`)
    * Array (`Array.isArray(x)`)
    * Date
    * RegExp
* symbols (ES2015)

notes:
     
    
    null & undefined
     (/!\ `typeof null === "object"`, comparer `maVariable === null`)